# Identification Bridge

This note records the determining-class bridge for `the Buzzard-Gee conjecture`. It fixes the endpoint representative, tracks `eps_coh`, and closes the final strict margin `M_BZG` in strict-coherence mode.
